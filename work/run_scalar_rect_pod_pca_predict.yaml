dataset: data/mode_decomp_eval_dataset_v1/scalar_rect
task: predict
pipeline:
  decomposer: pod_svd
  coeff_post: pca
  model: ridge
output:
  root: runs
  tag: scalar_rect_pod_pca
  project: scalar_rect_pod_pca
seed: 123
params:
  decompose:
    name: pod_svd
    mask_policy: ignore_masked_points
  model:
    target_space: z
task_params:
  train_run_dir: /Volumes/SP PX10/Main_code/mode_decomp/mode_decomp_greenfield_devkit_v1/runs/scalar_rect_pod_pca/train
