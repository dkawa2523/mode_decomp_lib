# TASK 511: Cleanup: 共通化・不要削除・deprecated整理（コード量を抑えレビュー容易化）

## 目的
追加実装によるコード増殖を抑え、共通化・不要削除・命名整理を行います。

## 作業内容
1. POD系の共通処理（reshape/stack、state保存、metrics）をユーティリティへ寄せる
2. 互換aliasの整理（deprecated経路を docs に明記）
3. 重複している古いPOD/SVD実装があれば統合 or 削除（使われていないものを消す）
4. docs/addons を既存docsへの参照に統合し、ドキュメント過多を防ぐ

## 受け入れ条件
- 同じ処理が複数箇所に存在しない（明らかな重複が減っている）
- 主要導線（run.yaml→train/eval/viz）が簡潔に読める
- 不要ファイルが削除されている

## 検証
- grepで重複が減っていることを確認
- 小さな実行を1回回して問題ない
