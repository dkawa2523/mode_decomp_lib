# S00: Repo Onboarding

## 目的
- 既存repoの入口・データI/O・config・評価を短時間で把握し、Processへ分解する。

## 手順
1) `tree -L 4`
2) 入口（scripts, main, cli）を列挙し、責務混在を確認
3) データI/O（CSV/SDF/DB）と主キー、目的変数を確定
4) 評価（split/seed/metric）を確定
5) docs/10 Process Catalog を更新（TODO含む）
