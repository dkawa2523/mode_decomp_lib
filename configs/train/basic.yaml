name: basic

eval:
  enabled: true
  metrics: [rmse, mae, r2]
  val_ratio: 0.2
  shuffle: true
  seed: ${seed}

cv:
  enabled: false
  folds: 5
  shuffle: true
  seed: ${seed}

tuning:
  enabled: false
  metric: rmse
  maximize: false
  param_grid: {}

viz:
  enabled: true
  max_dims: 6
  max_points: 2000
