name: autoencoder
mask_policy: zero_fill
include_mean: true
loss_masked: true
normalize: standardize

# Keep bench runs fast and deterministic.
latent_dim: 16
hidden_channels: [8, 16]
device: cpu
epochs: 50
batch_size: 16
lr: 1.0e-3
